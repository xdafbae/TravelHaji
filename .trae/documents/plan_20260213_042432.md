I will transform the current Laravel skeleton into the "Sistem Haji & Umrah" described in your plan. I will focus on a modern, responsive UI using Tailwind CSS and Alpine.js, avoiding external npm installations by using CDNs as requested.

### Phase 1: Database & Foundation
1.  **Database Migration**: 
    -   Create all 23 tables defined in the ERD (Jamaah, Passport, Embarkasi, Kas, Transaksi, etc.).
    -   Setup foreign key constraints for data integrity.
    -   Seed initial data (Default Roles, Admin User, Payment Methods).
2.  **Models**: 
    -   Generate Eloquent models for all tables.
    -   Define relationships (e.g., `Jamaah` hasOne `Passport`, `Embarkasi` hasMany `Jamaah`).

### Phase 2: Modern UI/UX Architecture
1.  **Layout System**:
    -   Create a modern **Dashboard Layout** with a responsive Sidebar and Topbar.
    -   Use **Tailwind CSS** for professional styling (Emerald/Teal theme).
    -   Use **Alpine.js** for interactive elements (Dropdowns, Modals, Sidebar toggles).
2.  **Asset Integration (CDN)**:
    -   **FontAwesome**: For intuitive icons.
    -   **SweetAlert2**: For beautiful success/error popups.
    -   **Chart.js**: For dashboard analytics.

### Phase 3: Authentication & Role Management
1.  **Custom Auth**: Adapt Laravel Breeze to use the `pegawai` table structure (Username/Password login).
2.  **Role Middleware**: Implement access control based on the defined roles (Super Admin, Kasir, etc.).

### Phase 4: Key Modules Implementation
1.  **Dashboard**:
    -   Summary cards (Total Jamaah, Cash Flow, Upcoming Departures).
    -   Quick action buttons.
2.  **Jamaah Management (Pendaftaran)**:
    -   **List View**: Modern data table with search, filters, and status badges.
    -   **Registration Form**: A **Multi-step Wizard** (Step 1: Personal, Step 2: Contact, Step 3: Documents) for a "very good UX".
    -   **Detail View**: Tabbed interface to see Profile, Passport, and Payment history.

I will start by setting up the database and the main layout.
